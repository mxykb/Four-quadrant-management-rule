<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/permission_notification_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/permission_bar_background"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:visibility="gone"
    android:elevation="4dp">

    <ImageView
        android:id="@+id/permission_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_warning"
        android:layout_marginEnd="8dp"
        android:tint="@color/permission_warning_color" />

    <HorizontalScrollView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:scrollbars="none"
        android:fadingEdge="horizontal"
        android:fadingEdgeLength="16dp">

        <TextView
            android:id="@+id/permission_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="应用需要通知权限以正常工作"
            android:textSize="13sp"
            android:textColor="@color/permission_text_color"
            android:singleLine="true"
            android:ellipsize="none" />

    </HorizontalScrollView>

    <ImageButton
        android:id="@+id/permission_close_btn"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_close"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:layout_marginStart="8dp"
        android:tint="@color/permission_text_color"
        android:contentDescription="关闭" />

</LinearLayout>